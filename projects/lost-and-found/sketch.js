function setup() {
  createCanvas(600, 400);
  background(30);

  fill(220);
  textAlign(CENTER, CENTER);
  textSize(32);
  text("Lost and Found", width / 2, height / 2);
}

function draw() {
  // 暂时留空
}
